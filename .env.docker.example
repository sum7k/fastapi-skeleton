# ====================================
# Docker Compose Environment Variables
# ====================================
# Copy this file to .env.docker and update values as needed
# Use with: docker-compose --env-file .env.docker up

# Application Configuration
APP_PORT=8000
APP_PROD_PORT=8001
LOG_LEVEL=INFO

# JWT Authentication
# IMPORTANT: Generate a secure key with: openssl rand -hex 32
JWT_SECRET_KEY=your-super-secret-key-here-minimum-32-characters-long

# PostgreSQL Configuration
POSTGRES_PORT=5432

# Jaeger / OpenTelemetry Configuration
JAEGER_OTLP_PORT=4318
JAEGER_UI_PORT=16686
JAEGER_GRPC_PORT=4317

# Note: Internal Docker network URLs are defined in docker-compose.yml
# - App connects to: postgresql+asyncpg://postgres:postgres@db:5432/fa_skeleton
# - App sends traces to: http://jaeger:4318
